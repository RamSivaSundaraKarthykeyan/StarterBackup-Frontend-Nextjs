import React from "react";
import EmailCard from "../(components)/EmailCard";
import Foot from "../(components)/Foot";

const page = () => {
  return (
    <div>
      invest
      <EmailCard />
      <Foot />
    </div>
  );
};

export default page;
